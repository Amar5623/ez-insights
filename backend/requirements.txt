# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# LLM providers
groq==0.9.0                   # Groq — default LLM (fast, free)
google-generativeai==0.5.4    # Gemini LLM
requests==2.32.2              # Ollama HTTP calls

# ── Embedders ─────────────────────────────────────────────────────────────────
# nomic-embed-text runs via Ollama (uses requests above — no extra package)

# ── OPTION A: Cohere embedder (EMBEDDER_PROVIDER=cohere) ──────────────────────
# Uncomment this block and comment out Option B when using Cohere.
# NOTE: Cohere requires tokenizers<0.20 — incompatible with Gemma below.
cohere==5.5.0
# tokenizers>=0.19,<0.20        # pinned by cohere (auto-installed)

# ── OPTION B: Gemma embedder (EMBEDDER_PROVIDER=gemma) ────────────────────────
# Uncomment this block and comment out Option A when using Gemma.
# Requires transformers>=4.51 for gemma3_text architecture support.
# sentence-transformers>=3.0.0
# transformers>=4.51.0
# huggingface-hub>=1.3.0
# tokenizers>=0.21              # required by transformers>=4.51
# ──────────────────────────────────────────────────────────────────────────────

# Databases
PyMySQL==1.1.0
pymongo==4.7.2
DBUtils==3.1.0                # MySQL connection pool

# Vector stores
faiss-cpu==1.8.0
pinecone-client==4.1.0        # optional — only if VECTOR_STORE=pinecone

# Strategy utilities
python-Levenshtein==0.25.1
sqlparse==0.5.0
numpy==1.26.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                 # async test client for FastAPI